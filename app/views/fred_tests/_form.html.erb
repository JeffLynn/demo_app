<%= form_for(@fred_test) do |f| %>
  <% if @fred_test.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@fred_test.errors.count, "error") %> prohibited this fred_test from being saved:</h2>

      <ul>
      <% @fred_test.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :content %><br />
    <%= f.text_field :content %>
  </div>
  <div class="field">
    <%= f.label :user_id %><br />
    <%= f.number_field :user_id %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
